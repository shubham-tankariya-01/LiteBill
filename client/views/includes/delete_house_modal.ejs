<!-- Delete House Confirmation Modal -->
<div id="deleteHouseModal" class="modal hidden" style="z-index: 10000;">
    <div class="modal-content-new" style="max-width: 400px; text-align: center; padding: 2.5rem 2rem;">
        <button type="button" class="modal-close-btn" onclick="closeDeleteHouseModal()">
            <i data-lucide="x" style="width: 20px; height: 20px;"></i>
        </button>

        <div style="width: 64px; height: 64px; background: rgba(239, 68, 68, 0.1); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem auto; border: 1px solid rgba(239, 68, 68, 0.2);">
            <i data-lucide="alert-triangle" style="color: var(--accent-red); width: 32px; height: 32px;"></i>
        </div>
        
        <h2 style="margin: 0 0 0.5rem 0; font-size: 1.5rem; font-weight: 800; color: var(--text-primary);">Are you sure?</h2>
        <p class="text-secondary" style="margin-bottom: 2rem; font-size: 0.95rem; line-height: 1.5;">
            You are about to delete <strong id="deleteHouseName" style="color: var(--text-primary);">this house</strong>. 
            This action cannot be undone and will permanently erase all associated rooms, bills, and history.
        </p>

        <form id="deleteHouseForm" method="POST" action="">
            <div style="display: flex; gap: 1rem;">
                <button type="button" class="btn btn-secondary" style="flex: 1;" onclick="closeDeleteHouseModal()">Cancel</button>
                <button type="submit" class="btn btn-primary" style="flex: 1.5; background: var(--accent-red); color: white; border: none;">Delete Permanently</button>
            </div>
        </form>
    </div>
</div>

<script>
    function openDeleteHouseModal(houseId, houseName) {
        document.getElementById('deleteHouseName').innerText = houseName;
        document.getElementById('deleteHouseForm').action = `/houses/${houseId}?_method=DELETE`;
        document.getElementById('deleteHouseModal').classList.remove('hidden');
        document.body.classList.add('modal-open');
    }

    function closeDeleteHouseModal() {
        document.getElementById('deleteHouseModal').classList.add('hidden');
        document.body.classList.remove('modal-open');
    }
</script>
