<nav class="navbar top-navbar">
    <div class="brand">
        <a href="/dashboard" style="display: flex; align-items: center; gap: 0.5rem; text-decoration: none;">
            <i data-lucide="zap" style="color: var(--accent-orange);"></i>
            <span style="color: var(--text-main); font-weight: 800; font-size: 1.5rem; letter-spacing: -0.5px;">LiteBill</span>
        </a>
    </div>

    <div class="nav-links" id="navLinks">
        <a href="/dashboard" class="nav-item"><i data-lucide="layout-dashboard" style="color: var(--text-muted);"></i> Dashboard</a>
        
        <!-- Properties Dropdown -->
        <div class="dropdown-container" style="position: relative;">
            <a href="#" id="propertiesDropdownBtn" class="nav-item">
                <i data-lucide="home" style="color: var(--text-muted);"></i> Properties <i data-lucide="chevron-down" style="width: 14px; margin-left: 2px;"></i>
            </a>
            <div id="propertiesDropdown" class="nav-dropdown-menu hidden" style="left: 0; right: auto; transform-origin: top left;">
                <div class="dropdown-header">Manage Properties</div>
                <a href="/houses" class="dropdown-item">
                    <i data-lucide="list" style="width: 14px; color: var(--accent-yellow);"></i>
                    View All Properties
                </a>
                <a href="/houses/new" class="dropdown-item">
                    <i data-lucide="plus-circle" style="width: 14px; color: #10B981;"></i>
                    Add New Property
                </a>
                <% if (typeof navHouses !== 'undefined' && navHouses.length > 0) { %>
                    <div class="dropdown-header" style="margin-top: 0.5rem; border-top: 1px solid var(--border-soft);">Quick Access</div>
                    <% navHouses.slice(0, 3).forEach(house => { %>
                        <a href="/houses/<%= house._id %>" class="dropdown-item" style="font-size: 0.85rem;">
                            <i data-lucide="arrow-right" style="width: 12px; color: var(--text-light);"></i>
                            <%= house.house_name %> Dashboard
                        </a>
                    <% }) %>
                <% } %>
            </div>
        </div>

        <% if (typeof navHouses !== 'undefined' && navHouses.length === 1) { %>
            <a href="/houses/<%= navHouses[0]._id %>/cycles" class="nav-item">
                <i data-lucide="calendar-clock" style="color: var(--text-muted);"></i> Billing Cycles
            </a>
        <% } else if (typeof navHouses !== 'undefined' && navHouses.length > 1) { %>
            <div class="dropdown-container" style="position: relative;">
                <a href="#" id="cyclesDropdownBtn" class="nav-item">
                    <i data-lucide="calendar-clock" style="color: var(--text-muted);"></i> Billing Cycles <i data-lucide="chevron-down" style="width: 14px; margin-left: 2px;"></i>
                </a>
                <div id="cyclesDropdown" class="nav-dropdown-menu hidden" style="left: 0; right: auto; transform-origin: top left;">
                    <div class="dropdown-header">Billing Cycles</div>
                    <% navHouses.forEach(house => { %>
                        <a href="/houses/<%= house._id %>/cycles" class="dropdown-item">
                            <i data-lucide="home" style="width: 14px; color: var(--accent-orange);"></i>
                            <%= house.house_name %>
                        </a>
                    <% }) %>
                </div>
            </div>
        <% } %>
        <% if (typeof navHouses !== 'undefined' && navHouses.length === 1) { %>
            <a href="/houses/<%= navHouses[0]._id %>/history" class="nav-item">
                <i data-lucide="history" style="color: var(--text-muted);"></i> History
            </a>
        <% } else if (typeof navHouses !== 'undefined' && navHouses.length > 1) { %>
            <div class="dropdown-container" style="position: relative;">
                <a href="#" id="historyDropdownBtn" class="nav-item">
                    <i data-lucide="history" style="color: var(--text-muted);"></i> History <i data-lucide="chevron-down" style="width: 14px; margin-left: 2px;"></i>
                </a>
                <div id="historyDropdown" class="nav-dropdown-menu hidden" style="left: 0; right: auto; transform-origin: top left;">
                    <div class="dropdown-header">Billing History</div>
                    <% navHouses.forEach(house => { %>
                        <a href="/houses/<%= house._id %>/history" class="dropdown-item">
                            <i data-lucide="home" style="width: 14px; color: var(--accent-orange);"></i>
                            <%= house.house_name %>
                        </a>
                    <% }) %>
                </div>
            </div>
        <% } %>
    </div>

    <div class="nav-actions" id="navActions">
        
        <!-- Add Menu Dropdown -->
        <div class="dropdown-container" style="position: relative;">
            <button id="navCreateBtn" class="btn btn-outline" style="font-size: 0.8rem; padding: 0.5rem 1rem; border-color: var(--border-soft); background: white;">
                <i data-lucide="plus" style="width: 14px; color: var(--text-main);"></i> Create <i data-lucide="chevron-down" style="width: 14px; margin-left: 4px;"></i>
            </button>
            <div id="navCreateDropdown" class="nav-dropdown-menu hidden" style="right: 0; transform-origin: top right;">
                <div class="dropdown-header">Actions</div>
                <a href="/houses/new" class="dropdown-item"><i data-lucide="home" style="width: 14px; color: var(--accent-yellow);"></i> New Property</a>
                <% if (typeof navHouses !== 'undefined' && navHouses.length > 0) { %>
                    <a href="/houses/<%= navHouses[0]._id %>/rooms" class="dropdown-item"><i data-lucide="bed-double" style="width: 14px; color: var(--accent-pink);"></i> Manage Rooms</a>
                <% } %>
            </div>
        </div>

        <% if (typeof navHouses !=='undefined' && navHouses.length===1) { %>
            <a href="/houses/<%= navHouses[0]._id %>/main-bill/new" class="btn btn-primary"
                style="font-size: 0.8rem; padding: 0.5rem 1.25rem; background: #10B981; border-color: #10B981;">
                <i data-lucide="pen-line" style="width: 14px;"></i> Submit Readings
            </a>
        <% } else if (typeof navHouses !=='undefined' && navHouses.length> 1) { %>
            <div class="dropdown-container" style="position: relative;">
                <button id="navSubmitReadingsBtn" class="btn btn-primary"
                    style="font-size: 0.8rem; padding: 0.5rem 1.25rem; background: #10B981; border-color: #10B981; cursor: pointer;">
                    <i data-lucide="pen-line" style="width: 14px;"></i> Submit Readings <i data-lucide="chevron-down" style="width: 14px; margin-left: 4px;"></i>
                </button>
                <div id="navReadingsDropdown" class="nav-dropdown-menu hidden" style="right: 0; transform-origin: top right;">
                    <div class="dropdown-header">Select Property</div>
                    <% navHouses.forEach(house=> { %>
                        <a href="/houses/<%= house._id %>/main-bill/new" class="dropdown-item">
                            <i data-lucide="home" style="width: 14px; color: var(--accent-yellow);"></i>
                            <%= house.house_name %>
                        </a>
                    <% }) %>
                </div>
            </div>
        <% } else { %>
            <a href="/houses/new" class="btn btn-primary" style="font-size: 0.8rem; padding: 0.5rem 1.25rem; background: #10B981; border-color: #10B981;"
                title="Add a house first">
                <i data-lucide="plus" style="width: 14px;"></i> Add First Property
            </a>
        <% } %>

        <!-- User Avatar Dropdown -->
        <div class="dropdown-container" style="position: relative;">
            <a href="#" id="userAvatarBtn" class="user-avatar" style="background: linear-gradient(135deg, var(--accent-orange), var(--accent-pink));">
                <i data-lucide="user" style="width: 18px; color: #FFF;"></i>
            </a>
            <div id="userAvatarDropdown" class="nav-dropdown-menu hidden" style="right: 0; transform-origin: top right; width: 200px;">
                <div class="dropdown-header">My Account</div>
                <a href="/auth/profile" class="dropdown-item">
                    <i data-lucide="user" style="width: 14px; color: var(--accent-orange);"></i> My Profile
                </a>
                <form action="/auth/logout" method="POST" style="margin: 0;">
                    <button type="submit" class="dropdown-item" style="width: 100%; border: none; background: none; cursor: pointer; font-family: var(--font-main); text-align: left; color: #EF4444;">
                        <i data-lucide="log-out" style="width: 14px; color: #EF4444;"></i> Sign Out
                    </button>
                </form>
            </div>
        </div>
    </div>

    <button id="mobileMenuBtn" class="hamburger-btn">
        <i data-lucide="menu" style="width: 24px; color: var(--text-main);"></i>
    </button>
</nav>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        // Dropdown toggle function
        const setupDropdown = (btnId, menuId) => {
            const btn = document.getElementById(btnId);
            const menu = document.getElementById(menuId);
            if (btn && menu) {
                btn.addEventListener("click", (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    document.querySelectorAll('.nav-dropdown-menu').forEach(m => {
                        if (m !== menu) {
                            m.classList.add('hidden');
                            m.style.top = ''; m.style.bottom = ''; m.style.marginTop = ''; m.style.marginBottom = '';
                        }
                    });
                    
                    if (menu.classList.contains("hidden")) {
                        menu.classList.remove("hidden");
                        const rect = btn.getBoundingClientRect();
                        if ((window.innerHeight - rect.bottom) < 260) {
                            menu.style.top = 'auto'; menu.style.bottom = '100%';
                            menu.style.marginTop = '0'; menu.style.marginBottom = '0.75rem';
                            menu.style.transformOrigin = 'bottom left';
                        } else {
                            menu.style.top = '100%'; menu.style.bottom = 'auto';
                            menu.style.marginTop = '0.75rem'; menu.style.marginBottom = '0';
                            menu.style.transformOrigin = 'top left';
                        }
                    } else {
                        menu.classList.add("hidden");
                    }
                });
                document.addEventListener("click", (e) => {
                    if (!menu.contains(e.target) && !btn.contains(e.target)) {
                        menu.classList.add("hidden");
                    }
                });
            }
        };

        setupDropdown("cyclesDropdownBtn", "cyclesDropdown");
        setupDropdown("navSubmitReadingsBtn", "navReadingsDropdown");
        setupDropdown("historyDropdownBtn", "historyDropdown");
        setupDropdown("propertiesDropdownBtn", "propertiesDropdown");
        setupDropdown("navCreateBtn", "navCreateDropdown");
        setupDropdown("userAvatarBtn", "userAvatarDropdown");

        // Hamburger toggle
        const hamburgerBtn = document.getElementById("mobileMenuBtn");
        const navLinks = document.getElementById("navLinks");
        const navActions = document.getElementById("navActions");
        if (hamburgerBtn && navLinks && navActions) {
            hamburgerBtn.addEventListener("click", () => {
                navLinks.classList.toggle("show");
                navActions.classList.toggle("show");
            });
        }
        
        // Re-initialize lucide icons inside the dynamically loaded scripts if needed
        if (typeof lucide !== 'undefined') {
            lucide.createIcons();
        }
    });
</script>

<style>
    .nav-dropdown-menu {
        position: absolute;
        top: 100%;
        margin-top: 0.75rem;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        border: 1px solid var(--border-soft);
        width: 240px;
        max-height: 260px;
        overflow-y: auto;
        z-index: 1000;
        animation: fadeInDown 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
    }

    /* Custom Scrollbar for Dropdown */
    .nav-dropdown-menu::-webkit-scrollbar {
        width: 6px;
    }
    .nav-dropdown-menu::-webkit-scrollbar-track {
        background: transparent;
    }
    .nav-dropdown-menu::-webkit-scrollbar-thumb {
        background-color: var(--border-soft);
        border-radius: 10px;
    }

    .nav-dropdown-menu.hidden {
        display: none;
    }

    .dropdown-header {
        padding: 0.85rem 1.25rem;
        font-size: 0.7rem;
        font-weight: 800;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 1px;
        background: #F4F6F5;
        border-bottom: 1px solid var(--border-soft);
    }

    .dropdown-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.85rem 1.25rem;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--text-main);
        text-decoration: none;
        transition: all 0.2s ease;
    }

    .dropdown-item:hover {
        background: rgba(254, 138, 92, 0.05);
        color: var(--accent-orange);
        padding-left: 1.5rem;
    }

    @keyframes fadeInDown {
        from {
            opacity: 0;
            transform: translateY(-10px) scale(0.95);
        }

        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }
</style>